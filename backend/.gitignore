1. Dependency directories

node_modules/

2. Build outputs

dist/
build/

3. Environment variables (TUYỆT ĐỐI QUAN TRỌNG)

.env
.env.production
.env.test

4. Logs

npm-debug.log*
yarn-debug.log*
yarn-error.log*
logs/
*.log

5. IDE & System files

.idea/
.vscode/
*.swp
.DS_Store
Thumbs.db

6. Testing coverage

coverage/

7. Local Uploads (Vì đã dùng Cloudinary, không push ảnh test local lên)

uploads/

8. Prisma generated client (Nên generate lại khi clone)

(Tùy chọn: Thường ta không ignore cái này nếu muốn dev nhanh, nhưng best practice là ignore)

/prisma/generated/